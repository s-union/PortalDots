# Scenarios

Each scenario should define:

- actor
- session state
- request
- expected authorization result
- expected state transition
- expected artifact or response contract

The future parity runner will execute the same scenario against both runtimes.
